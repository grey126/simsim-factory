# 심심공장 작업 규칙
- sources와 사용자 개인 파일을 이 저장소에 복사하지 않는다.
- 브라우저에서만 동작하는 HTML/CSS/JS/JSON을 유지한다.
- 서버, 계정, 데이터베이스, 외부 API, 추적·광고 스크립트는 별도 요청 없이 추가하지 않는다.
- 콘텐츠 원본은 content/*.json이다. 질문과 결과는 한국어 창작 콘텐츠로 작성한다.
- 기존 v1 친구 퀴즈의 문항·선택지 순서를 보존한다. 공유 링크 호환이 우선이다.
- 입력 문자열은 HTML에 넣기 전에 이스케이프한다. fragment는 길이, 버전, 답변 범위를 검증한다.
- npm test 및 npm run build 후 모바일·데스크톱과 공유 경로를 확인한다.
- 배포 대상은 dist만이다. 로컬 파일, 인증 정보, qa 산출물을 게시하지 않는다.
- 자동 SNS 게시·유료 서비스·수익화 변경에는 사용자의 별도 지시가 필요하다.
