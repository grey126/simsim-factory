# 오루완 작업 규칙
- sources와 사용자 개인 파일을 이 저장소에 복사하지 않는다.
- 브라우저에서만 동작하는 HTML/CSS/JS/JSON을 유지한다.
- 서버, 계정, 데이터베이스, 외부 API, 추적·광고 스크립트는 별도 요청 없이 추가하지 않는다.
- 콘텐츠 원본은 content/*.json이다. 질문과 결과는 한국어 창작 콘텐츠로 작성한다.
- 기존 v1 친구 퀴즈의 문항·선택지 순서를 보존한다. 공유 링크 호환이 우선이다.
- 입력 문자열은 HTML에 넣기 전에 이스케이프한다. fragment는 길이, 버전, 답변 범위를 검증한다.
- npm test 및 npm run build 후 모바일·데스크톱과 공유 경로를 확인한다.
- 배포 대상은 dist만이다. 로컬 파일, 인증 정보, qa 산출물을 게시하지 않는다.
- 자동 SNS 게시·유료 서비스·수익화 변경에는 사용자의 별도 지시가 필요하다.
- 새 이미지는 전형적인 AI 생성물처럼 보이지 않게 만든다. 실제 소재가 필요하면 사용 가능한 실사 사진을 우선하고, 창작 이미지는 사람의 편집 흔적이 느껴지는 강한 한글 타이포그래피, 제한된 색상, 인쇄·종이 질감, 의도적인 크롭과 작은 불균형을 사용한다.
- 매끈한 3D 캐릭터, 플라스틱 같은 피부, 몽환적 광원, 과도한 그라데이션, 의미 없는 장식물, 가짜 글자, 지나치게 완벽한 대칭, 프롬프트 요소를 한 화면에 나열한 구성을 피한다. 사용자가 명시적으로 요청한 경우는 예외다.
- 이미지 모델에 한글 문구를 직접 그리게 하지 않는다. 가능한 경우 이미지는 글자 없이 만들고 정확한 문구는 HTML, Canvas 또는 일반 편집 도구로 나중에 합성한다.
- 생성 이미지는 사용 전 직접 검수한다. 손·사물의 형태, 글자, 그림자, 반복 무늬, 원근과 재질이 부자연스럽거나 전형적인 AI 스톡 이미지처럼 보이면 사용하지 않는다.
- 기본 캐릭터 자산은 `scripts/create-mascots.mjs`가 만드는 단순 2D 벡터 마스코트를 사용한다. 큰 둥근 머리, 작은 몸, 점 눈, 짧은 팔다리와 제한된 색을 유지하며 사실적인 인체·피부·손 묘사를 피한다.
- 브랜드 표기는 한글 `오루완`을 우선하고 `OLTD`는 로고·스탬프의 보조 표기로 사용한다. 의미 설명이 필요한 곳에서는 `오늘도 루팡 완료` 또는 `가벼운 캐릭터 테스트`를 함께 쓴다.
- 설문 문항은 진지한 자기진단보다 일상·친구·취향·상상처럼 가벼운 화제를 다룬다. 선택지는 한 번 읽고 바로 고를 수 있게 짧고 구체적으로 쓴다.
- 성격을 나누는 테스트는 결과마다 말하기 쉬운 핵심 키워드와 고유 캐릭터를 제공하고, 가능하면 8~16개 유형을 구성한다. 유형 수만 늘리고 차이가 흐려지지 않도록 행동 기준과 캐릭터 실루엣을 분명히 구분한다.
